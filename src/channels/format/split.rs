//! Code-block-aware splitter. Filled in Task 5.
use super::RenderedBlock;
pub fn split(_blocks: &[RenderedBlock], _limit: usize) -> Vec<String> {
    Vec::new()
}
pub fn split_paired(
    _primary: &[RenderedBlock],
    _fallback: &[RenderedBlock],
    _limit: usize,
) -> Vec<(String, String)> {
    Vec::new()
}
pub fn join_all(_blocks: &[RenderedBlock]) -> String {
    String::new()
}
